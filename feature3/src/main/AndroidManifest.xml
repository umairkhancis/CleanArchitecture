<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.feature3">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_feature3">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application
        android:usesCleartextTraffic="true">
        <activity android:name="feature3.view.activity.MoviesListActivity">
            <intent-filter>
                <action android:name="com.noorifytech.feature3.view.activity.MoviesListActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name="feature3.view.activity.MovieDetailsActivity">
            <intent-filter>
                <action android:name="com.noorifytech.feature3.view.activity.MovieDetailsActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>

